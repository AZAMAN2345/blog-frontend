import { useState, useEffect } from "react";

const Effect = () => {
  const [count, setCount] = useState(0);
  const title = "Title";
  useEffect(() => {
;
  });
  return <h1>TITLE</h1>;
};
export default Effect;
